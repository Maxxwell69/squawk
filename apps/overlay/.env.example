# Postgres (Railway plugin, Neon, etc.)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"

AUTH_SECRET=
# Must match the first captain’s email exactly (case-insensitive) for one-time bootstrap register
ADMIN_EMAIL=captain@example.com
