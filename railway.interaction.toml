# Crew portal (Next.js): magic-link email, admin adds moderators.
# Railway: add a **separate** service → Settings → Config-as-code → `railway.interaction.toml`
# Add Postgres plugin (or DATABASE_URL), AUTH_SECRET, AUTH_URL, ADMIN_EMAIL, EMAIL_SERVER, EMAIL_FROM.

[build]
builder = "dockerfile"
dockerfilePath = "apps/interaction/Dockerfile"

[deploy]
restartPolicyType = "on_failure"
